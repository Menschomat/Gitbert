# git_bot configuration file template (config.toml)

# Server options
host = "0.0.0.0"
port = 8080
max_concurrent_reviews = 5

# Review options: "advisory" (non-blocking comments) or "enforcing" (approve/request changes)
review_mode = "advisory"
# Comment trigger mode: "autonomous" (Option B: smart helpful replies, default) or "mention_only" (Option A)
comment_trigger_mode = "autonomous"
# Action / CI coordination options
await_actions_completion = true
diagnose_action_failures = true
bot_name = "Gitbert"

# Model options
# Provider: "gemini" (native Google GenAI) or "litellm" (OpenAI-compatible / OpenRouter)
model_provider = "gemini"

# Native Gemini options
model_name = "gemini-3.8-flash"
# google_api_key = "your-gemini-api-key"

# Agnostic OpenAI-compatible / OpenRouter / vLLM options (when model_provider = "litellm")
openai_compatible_model = "openrouter/anthropic/claude-3.5-sonnet"
openai_compatible_api_base = "https://openrouter.ai/api/v1"
# openai_compatible_api_key = "your-openrouter-api-key"

# Gitea platform options
gitea_url = "http://localhost:3000"
# gitea_token = "your-gitea-token"
# gitea_webhook_secret = "your-webhook-secret"
